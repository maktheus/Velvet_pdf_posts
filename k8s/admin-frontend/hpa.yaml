apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: admin-frontend
  namespace: patadeveludo
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: admin-frontend
  minReplicas: 1
  maxReplicas: 3
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
